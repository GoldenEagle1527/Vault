name: vault_agent_core
description: >
  Vault-owned fork of dart_agent_core (Memex Lab). Stateful tool-using AI agents
  for Flutter. Not published — see UPSTREAM.md.
version: 2.0.4
publish_to: none
# Provenance only; this tree is maintained inside the Vault repo.
repository: https://github.com/memex-lab/dart_agent_core
homepage: https://github.com/memex-lab/dart_agent_core
issue_tracker: https://github.com/memex-lab/dart_agent_core/issues
topics:
  - ai
  - agent
  - agent-framework
  - llm
  - flutter

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  aws_common: ^0.7.12
  aws_signature_v4: ^0.6.10
  crypto: ^3.0.7
  dio: ^5.9.0
  http: ^1.6.0
  logging: ^1.3.0
  uuid: ^4.5.2
  # path: ^1.9.0

dev_dependencies:
  build_runner: ^2.11.0
  lints: ^6.0.0
  mockito: ^5.6.3
  test: ^1.25.6
